<!DOCTYPE html>
<html lang="en" class="scroll-smooth">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ArteLu | Academia de Deportes Aéreos</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: "#52054d",
                        secondary: "#2f328a",
                        accent: "#bca0dd",
                        highlight: "#ab5b9f",
                        lightbg: "#efefdc",
                        neutralgray: "#d9d9d9",
                    }
                }
            }
        }
    </script>


    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <link rel="stylesheet" href="/css/styles.css" />

    <link rel="icon" href="/assets/logo.png" type="Logo de ArteLu" />
</head>

<body
    class="flex flex-col min-h-screen bg-lightbg text-primary dark:bg-gray-900 dark:text-neutralgray transition-colors duration-500">

    <%- include('./partials/navbar.ejs') %>
        <main class="flex-grow py-24">
            <section
                class="bg-[url('/assets/logo.png')] bg-opacity-80 bg-no-repeat bg-center h-96 flex items-center justify-center">

            </section>
            <div class="bg-secondary bg-opacity-50 p-6 rounded flex items-center justify-center mb-12 mx-6">
                <h1 class="text-4xl font-bold text-white">Equipo ArteLu</h1>
            </div>

            <section class="max-w-6xl mx-auto px-6 py-12">
                <h2 class="text-3xl font-semibold mb-6 text-center">Conoce a Nuestro Equipo</h2>
            </section>

            <section class="flex flex-col gap-12 px-6 max-w-5xl mx-auto">
                <% if (equipo && equipo.length> 0) { %>
                    <% equipo.forEach(docente=> { %>
                        <div
                            class="flex flex-col md:flex-row bg-white dark:bg-gray-800 rounded-2xl shadow-lg overflow-hidden border border-accent/20 group items-center md:items-start">

                            <div class="w-full m-6 md:w-1/3 aspect-[4/5] md:aspect-square overflow-hidden">
                                <img src="<%= docente.imagenes && docente.imagenes.url ? docente.imagenes.url : '/assets/placeholder-user.png' %>"
                                    alt="<%= docente.nombre %>"
                                    class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105">
                            </div>

                            <div class="w-full md:w-2/3 p-8 flex flex-col justify-center">
                                <div class="mb-4">
                                    <h3 class="text-3xl font-bold text-primary dark:text-accent">
                                        <%= docente.nombre %>
                                    </h3>
                                    <p class="text-secondary dark:text-highlight font-semibold italic">
                                        <%= docente.cargo %>
                                    </p>
                                </div>

                                <p class="text-gray-600 dark:text-gray-300 text-base leading-relaxed"
                                    style="white-space: pre-line;">
                                    <%= docente.descripcion %>
                                </p>
                            </div>
                        </div>
                        <% }) %>
                            <% } else { %>
                                <p class="text-center text-gray-500">Próximamente conocerás a nuestro equipo...</p>
                                <% } %>
            </section>
        </main>
        <%- include('./partials/footer.ejs') %>
</body>